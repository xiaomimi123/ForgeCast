# 开源变现内容工厂 — 内部开发文档

> 代号:**ForgeCast**(暂定,可改)
> 版本:v1.0 | 日期:2026-07-11
> 执行方式:本文档面向 Claude Code 执行,按里程碑逐模块交付。
> 一句话定义:一条「筛选开源项目 → 换皮成自有产品 → 批量生成小红书/抖音素材(文案+配图+视频)→ 引流接定制单」的个人内容生产流水线。

---

## 0. 设计原则(先读)

1. **本地 Web 控制台 + 引擎分离,整体跑在本地 Docker 里。** 这是给一个人用的生产工具,不是 SaaS——但日常操作走 Web UI(看板、审稿、素材预览),不走命令行。架构上引擎(pipeline 各模块)与界面严格分离:所有能力封装为 core 函数,Web 端通过本地 API 调用,CLI 作为同一套函数的第二入口保留(方便 Claude Code 调试和脚本化)。程序以 Docker Compose 方式在本机运行,数据与素材通过 volume 落在宿主机目录,服务只绑 localhost,不做登录不做多租户;这样环境可复现、迁移服务器零改动,将来平台化时只需换掉 Web 层,引擎原封不动。
2. **AI 只出初稿,人做终审。** 文案、视频均为草稿产物,发布前人工过一遍。发布环节不自动化(平台风控红线)。
3. **每个环节的产物都是文件。** 项目分析是 markdown,文案是 markdown,视频是 mp4,全部落在项目目录里,可追溯、可重跑。
4. **模板沉淀优于单次生成。** 提示词模板、Remotion 模板是系统的核心资产,版本化管理,持续迭代。
5. **协议红线前置。** GPL/AGPL 项目在筛选阶段直接淘汰,不进入后续流水线。

---

## 1. 系统架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                      ForgeCast Pipeline                      │
│                                                              │
│  M1 发现筛选      M2 商业分析      M3 换皮改造                │
│  ┌─────────┐    ┌─────────┐    ┌─────────────┐             │
│  │ GitHub   │───▶│ AI 分析  │───▶│ Claude Code  │             │
│  │ 抓取+评分 │    │ 痛点/买家│    │ 改名/换UI/   │             │
│  │ 协议过滤  │    │ 钩子匹配 │    │ 部署demo     │             │
│  └─────────┘    └─────────┘    └─────────────┘             │
│                                        │                     │
│                    ┌───────────────────┤                     │
│                    ▼                   ▼                     │
│  M4 文案配图              M5 视频生成                          │
│  ┌──────────────┐       ┌──────────────────┐               │
│  │ 提示词模板库   │       │ Remotion Skill    │               │
│  │ 标题/正文/封面 │──────▶│ 3套竖屏模板        │               │
│  │ 配图生成      │       │ TTS配音 + 录屏合成  │               │
│  └──────────────┘       └──────────────────┘               │
│                    │                   │                     │
│                    ▼                   ▼                     │
│              M6 素材库与发布辅助                                │
│              ┌──────────────────┐                            │
│              │ 按项目归档 / 发布日历│                           │
│              │ 私域话术库          │                           │
│              └──────────────────┘                            │
└─────────────────────────────────────────────────────────────┘
```

### 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 运行时 | Node.js 20 + TypeScript, pnpm monorepo | 与 Remotion 同生态,Claude Code 最熟 |
| Web 控制台 | Vite + React + shadcn/ui + TanStack Query | 你最熟的栈;本地单页应用,无 SSR 需求 |
| 本地 API | Hono(挂在同一 Node 进程,localhost:4321) | 轻量,直接 import core 函数,天然复用引擎 |
| 运行环境 | Docker Compose(app + renderer 两个服务,见 §12) | 环境可复现;Remotion 渲染依赖(Chromium/ffmpeg)封死在镜像里,迁移零改动 |
| 数据 | SQLite(better-sqlite3)+ 项目文件目录 | 单人工具,零运维 |
| LLM 调用 | 统一走 aitoken.homes 中转(OpenAI 兼容格式) | 复用现有基础设施,成本可控可切换模型 |
| 视频 | Remotion 4.x + 官方 Agent Skills(`npx skills add remotion-dev/skills`) | 视频即代码,可模板化批量产出 |
| TTS | MiniMax(经中转站)/ Edge-TTS 兜底 | MiniMax 支持音色克隆;Edge-TTS 免费兜底 |
| 配图 | 图片生成 API(经中转站,复用 MediaPilot 已接的 provider)+ HTML 转图(satori/Playwright 截图) | 封面图用 HTML 模板渲染更稳定可控,AI 图为辅 |
| 方法论知识层 | dbskill(dontbesilent2025/dbskill):26 个商业/内容 Skills + 4176 条知识原子 | 双路接入:Claude Code 插件(人工环节)+ atoms.jsonl 入库做检索增强(流水线),详见 §5.6 |
| 录屏 | 人工 OBS 录制(不自动化) | 真实感素材,机器做不了 |
| 部署 demo | Cloudflare Pages/Workers | 现有偏好,零成本 |

### Monorepo 结构

```
forgecast/
├── packages/
│   ├── core/            # 数据模型、SQLite、配置、LLM client(走中转站)
│   ├── scout/           # M1 抓取与筛选
│   ├── analyst/         # M2 商业化分析
│   ├── copywriter/      # M4 文案与配图
│   ├── studio/          # M5 视频生成(Remotion 项目在此)
│   └── server/          # 本地 API(Hono):REST 封装各模块 + 静态托管 web 产物
├── apps/
│   └── web/             # M7 Web 控制台(Vite + React)
├── templates/
│   ├── prompts/         # 提示词模板库(见 §5,核心资产)
│   ├── remotion/        # 3套视频模板源码(见 §6)
│   ├── covers/          # 封面图 HTML 模板
│   └── knowledge/       # dbskill 知识包 md(vendor,由 sync 命令拉取,见 §5.6)
├── workspace/           # 每个项目一个目录(见 §2 数据模型)
├── forgecast.config.ts  # API keys、目标行业配置、评分权重
└── cli.ts               # 统一入口:forgecast <command>
```

### CLI 命令总表

> CLI 与 Web 控制台调用同一套 core 函数,CLI 保留用于 Claude Code 调试与批处理;日常操作以 Web 为主。

```bash
forgecast dev                      # 启动本地服务:API(:4321) + Web 控制台(:5173)
forgecast scout                    # M1: 抓取候选项目,评分入库
forgecast pick <repo>              # 将候选标记为立项,创建 workspace 目录
forgecast analyze <slug>           # M2: 生成商业化分析报告
forgecast rebrand <slug>           # M3: 输出换皮任务清单(交 Claude Code 执行)
forgecast copy <slug> --hook=<型>  # M4: 生成一组文案+封面图
forgecast video <slug> --tpl=<型>  # M5: 渲染视频
forgecast knowledge sync           # 同步 dbskill 上游:知识包 md + atoms.jsonl 重建索引
forgecast calendar                 # M6: 查看发布日历与素材库存
```

---

## 2. 数据模型

### SQLite 表

```sql
-- 候选项目池
CREATE TABLE candidates (
  id INTEGER PRIMARY KEY,
  repo TEXT UNIQUE NOT NULL,          -- owner/name
  url TEXT NOT NULL,
  license TEXT,                        -- SPDX id
  license_ok INTEGER,                  -- 1=MIT/Apache/BSD/MPL, 0=GPL系/无协议
  stars INTEGER, last_commit TEXT,
  tech_stack TEXT,                     -- JSON: ["react","node",...]
  score REAL,                          -- 综合评分 0-100
  score_detail TEXT,                   -- JSON,四维度分项
  status TEXT DEFAULT 'candidate',     -- candidate|picked|rejected|shipped
  created_at TEXT DEFAULT (datetime('now'))
);

-- 立项项目
CREATE TABLE projects (
  id INTEGER PRIMARY KEY,
  slug TEXT UNIQUE NOT NULL,           -- 换皮后产品代号,如 "kuaiketong"
  candidate_id INTEGER REFERENCES candidates(id),
  brand_name TEXT,                     -- 中文品牌名
  target_buyer TEXT,                   -- 买家画像,如 "外贸soho"
  demo_url TEXT,
  price_deploy INTEGER,                -- 部署价(元)
  price_custom INTEGER,                -- 定制起步价(元)
  stage TEXT DEFAULT 'analysis'        -- analysis|rebranding|producing|publishing|selling
);

-- 素材(文案/图/视频统一管理)
CREATE TABLE assets (
  id INTEGER PRIMARY KEY,
  project_id INTEGER REFERENCES projects(id),
  type TEXT NOT NULL,                  -- copy|cover|video
  hook TEXT,                           -- 钩子类型,见 §5
  file_path TEXT NOT NULL,
  status TEXT DEFAULT 'draft',         -- draft|approved|published
  published_at TEXT, platform TEXT,    -- xhs|douyin
  perf TEXT                            -- JSON: 曝光/赞/收藏/询单数(人工回填)
);
```

### workspace 目录约定(每项目)

```
workspace/<slug>/
├── analysis.md          # M2 产出:商业化分析报告
├── rebrand-plan.md      # M3 产出:换皮任务清单
├── raw/                 # 原始素材:OBS录屏、demo截图
├── copy/                # M4 产出:文案 markdown(一文件一篇)
├── covers/              # M4 产出:封面图 png
└── videos/              # M5 产出:成片 mp4 + 使用的 props.json
```

---

## 3. M1 — 项目发现与筛选(scout)

### 数据源

1. GitHub Trending(daily/weekly,语言过滤:TS/JS/Python/Go)
2. GitHub Search API:按 topic 定向搜索(见下),`stars:>300 pushed:>{6个月前}`
3. 手动投喂:`forgecast scout --add <url>`(刷到好项目随手入库)

定向 topic 白名单(围绕"小老板买得懂"的品类,可配置):
`crm, e-commerce, live-chat, booking, invoice, inventory, form-builder, dashboard, chatbot, link-in-bio, scheduling, pos, wiki, survey`

### 四维评分模型(总分100)

| 维度 | 权重 | 打分依据 |
|---|---|---|
| 协议可商用 | 一票否决 | MIT/Apache-2.0/BSD/MPL 通过;GPL/AGPL/SSPL/无LICENSE 直接 rejected |
| 换皮成本 | 30 | 技术栈匹配度(React/Node/Next 满分)、有无 Docker、i18n 支持、UI 是否主题化。用 LLM 读 README+目录结构打分 |
| 买家清晰度 | 40 | LLM 判断:能否一句话说清"什么老板会掏钱"。工具越垂直分越高(给餐饮的排队系统 > 通用笔记应用) |
| 内容可视性 | 30 | 有无好看的 UI 可演示。纯 CLI/纯后端工具低分——没画面就没内容 |

产出:`forgecast scout` 后打印 Top 20 排名表,人工 `pick`。

### 实现要点

- GitHub API 用 PAT,注意限速,增量抓取(候选池去重)
- LICENSE 检测:先看 API 返回的 license 字段,无则拉根目录 LICENSE 文件让 LLM 判断;**判断不了一律按不通过处理**
- 评分 LLM 调用统一封装在 `core/llm.ts`,模型可配置(默认用中转站里性价比最高的)

---

## 4. M2 — 商业化分析引擎(analyst)

输入:candidate 的 README 全文 + 目录树 + 官网截图(如有)。
输出:`workspace/<slug>/analysis.md`,固定结构:

```markdown
# <项目名> 商业化分析

## 一句话:这是给谁的什么
## 目标买家画像(主攻1个,备选2个)
   - 行业 / 规模 / 现在怎么解决这个问题 / 每年为此花多少钱
## 痛点清单(按付费意愿排序,每条注明"现状成本")
## 换皮方向建议
   - 品牌名候选×3 / 主打功能砍留建议 / 需要新增的1-2个本土化功能(如微信登录、对接抖店)
## 定价建议
   - 部署价 / 定制起步价 / 参照物(市面上同类SaaS年费)
## 钩子匹配(核心输出,直接喂给 M4)
   - 副业型钩子:适用度 高/中/低 + 一句示例标题
   - 信息差型钩子:同上
   - 接单故事型钩子:同上
   - 行业痛点型钩子:同上
## 风险
   - 协议细节 / 竞品是否已在小红书打这个点 / 交付难点
```

实现:一次 LLM 调用(长上下文模型),提示词模板在 `templates/prompts/analysis.md`。分析报告是后续所有素材生成的唯一上下文源——**M4/M5 不重新读原始 README,只读 analysis.md**,保证口径一致。

---

## 5. M4 — 文案与配图生成(copywriter)

> 本模块与 M3(换皮)并行:文案初稿可以在 demo 部署前先出。

### 5.1 四大钩子公式(固定资产,写进提示词模板)

| 钩子代号 | 公式 | 示例骨架 |
|---|---|---|
| `sideline` 副业型 | 我做了个X,躺着收钱 | 「我花X天做了个XX工具,现在每月躺赚XXXX」 |
| `infogap` 信息差型 | 老板花2万买的,成本其实只要X | 「别再花X万找外包了,这套XX系统成本不到XXX」 |
| `story` 接单故事型 | 接了个单,客户要XX,我X天搞定 | 「客户:能做个XX吗?我:等我一天(附聊天记录)」 |
| `pain` 行业痛点型 | XX行业还在用Excel,降维打击 | 「做XX的还在手动XX?这个工具直接把效率翻X倍」 |

### 5.2 内容配比(账号人设约束)

- 60% 产品演示(pain/infogap 钩子为主)
- 20% 收入/接单晒图(story/sideline)
- 20% 开发过程碎片(Claude Code 录屏,人设强化,不带货)

`forgecast calendar` 按此配比检查素材库存,缺哪类提示补哪类。

### 5.3 文案生成

命令:`forgecast copy <slug> --hook=pain --n=3`

- 输入:analysis.md + 钩子模板 + 平台参数(xhs/douyin)
- 单篇输出(markdown,存 `copy/`):
  - 标题×3(≤20字,含数字或对比)
  - 小红书正文(300-500字,口语化,分段带 emoji,**结尾预埋私域钩子**,见 5.5)
  - 抖音口播脚本(45-60秒,标注:前3秒钩子画面 / 中段演示节奏 / 结尾CTA)——此脚本同时是 M5 视频的输入
  - 封面文案(主标题+副标题,喂给封面生成)
  - 评论区运营:预埋提问×2 + 高频问题回复话术×3
- 提示词模板:`templates/prompts/copy-{hook}.md`,每个钩子一个文件,**模板中硬编码违禁词规避规则**(不出现"最""第一""保证赚钱"等广告法/平台敏感词,LLM 生成后再跑一遍本地敏感词表校验)

### 5.4 封面图生成

主方案:**HTML 模板渲染**(不是 AI 生图)——小红书爆款封面本质是"大字报+产品截图",HTML 精确可控:

- `templates/covers/` 下 3 套 HTML 模板(大字报型 / 截图+标注型 / 对比型),CSS 变量控制配色
- 渲染:Playwright 截图 1242×1660(小红书 3:4)
- 命令内置于 `forgecast copy`,自动用封面文案+demo 截图填槽渲染

辅方案:AI 生图(经中转站)只用于配图素材(场景图、表情包),不用于主封面。

### 5.5 私域动线(话术资产)

`templates/prompts/funnel.md` 维护统一话术库:

- 文案结尾钩子:「部署文档整理好了,需要的评论区扣1」/「同行交流群在简介」
- 评论区回复模板、引导到微信的中转话术
- **红线:文案中不出现微信号/二维码/价格**,全部私域再谈

### 5.6 dbskill 方法论集成(创作大脑)

引入 [dontbesilent2025/dbskill](https://github.com/dontbesilent2025/dbskill)(26 个商业/内容 Skills + 4176 条知识原子),为文案生成注入选题、钩子、IP 方法论纵深。双路接入:

**路径A — Claude Code 插件(人工环节,零开发)**

```bash
claude plugin marketplace add dontbesilent2025/dbskill
claude plugin install dbs@dontbesilent-skills
```

用于 P0 手动创作与日常审稿:`/dbs-content` 选题打磨、`/dbs-hook` 优化视频前 3 秒/前 20 秒、`/dbs-benchmark` 对标账号研究、`/dbs-diagnosis` 产品定价与买家诊断(可反哺 M2 分析报告)。

**路径B — 流水线检索增强(自动化,copywriter 内建)**

1. `forgecast knowledge sync`:拉取上游仓库,复制 `知识库/Skill知识包/` 下与内容相关的 md 到 `templates/knowledge/`;导入 `知识库/原子库/atoms.jsonl` 至 SQLite:

```sql
CREATE TABLE knowledge_atoms (
  id INTEGER PRIMARY KEY,
  source TEXT DEFAULT 'dbskill',
  topic TEXT, content TEXT NOT NULL,
  meta TEXT                          -- 原子其余字段原样存 JSON
);
CREATE VIRTUAL TABLE atoms_fts USING fts5(content, topic, content='knowledge_atoms');
```

2. `forgecast copy` 生成时的提示词组装顺序:钩子模板 + **知识包 md(content/hook 相关,作 system 背景)** + **FTS 检索 top-8 原子**(检索词 = 钩子类型关键词 + analysis.md 的行业/痛点词)+ analysis.md。轻量 RAG,不引入向量库;若检索质量不够,P2 再升级 embedding。
3. 上游更新频繁,sync 命令记录版本号,每次重建 FTS 索引,幂等可重跑。

**使用边界(合规)**:dbskill 是他人公开的知识资产,用于**内部创作提效**;不将其内容原样打包进对外销售的产品,不对外宣称为自有方法论,引入前确认仓库 LICENSE 条款并遵守(与 M1 的协议筛查同一纪律)。

---

## 6. M5 — 视频生成(studio)

### 6.1 环境

```bash
cd packages/studio
npx create-video@latest .        # Remotion 脚手架
npx skills add remotion-dev/skills   # 官方 Agent Skill,教会 Claude Code 写正确的 Remotion 代码
```

规格统一:1080×1920 竖屏,30fps,H.264,时长 15-60 秒。

### 6.2 三套模板(核心资产,先搭模板再谈批量)

每套模板是一个 Remotion Composition,**所有可变内容通过 props.json 注入**,渲染命令:

```bash
forgecast video <slug> --tpl=demo
# 内部执行: npx remotion render <Composition> out.mp4 --props=workspace/<slug>/videos/props.json
```

**模板A `demo` 产品演示型(30-60s,主力)**

| 段落 | 时长 | 内容 | 素材来源 |
|---|---|---|---|
| 钩子 | 0-3s | 大字痛点标题 + 强动效(弹入/故障风) | copy 的封面文案 |
| 痛点 | 3-8s | 2-3条现状吐槽,逐条弹出 | analysis.md 痛点清单 |
| 演示 | 8-45s | **OBS 真实录屏**为底,Remotion 叠加功能标注框、箭头、字幕 | raw/ 录屏 |
| 报价锚点 | 45-52s | 「外面做要X万 → 我这里XXX」对比动画 | analysis 定价 |
| CTA | 52-60s | 私域钩子文案 + 账号水印 | funnel 话术 |

**模板B `story` 接单故事型(20-30s)**
聊天记录气泡逐条弹出(props 传对话 JSON)→ 交付截图轮播 → 收款/后台数据特写 → CTA。全程 TTS 旁白。

**模板C `flash` 纯钩子型(15s,起量测试用)**
3秒痛点大字 → 产品界面快闪(3-4张截图卡点)→ 一句话卖点 → CTA。BGM 卡点,无旁白。

### 6.3 配音与字幕

- TTS:MiniMax(经 aitoken.homes,支持音色克隆,后期录自己的声音克隆增强人设);Edge-TTS 免费兜底
- 流程:口播脚本 → TTS 生成 wav + 时间戳 → Remotion `<Audio>` 挂载,字幕组件按时间戳逐句显示
- **抖音版必须硬字幕**(平台习惯),小红书版可选

### 6.4 剪辑层:videocut-skills(录屏→素材)

引入 [Ceeon/videocut-skills](https://github.com/Ceeon/videocut-skills)(口播视频剪辑 Skills 包),填补"原始录屏 → 可用素材"环节,与 Remotion 形成两层分工:

```
OBS 原始录屏(3-5分钟) → videocut(拆分镜/去废话/ASR字幕/竖屏导出)
                          → 精选片段 → Remotion(钩子动效/标注/CTA包装) → 成片
```

- 覆盖场景与账号内容形态完全重合:口播教程、产品演示、知识讲解、结果展示
- Agent 工作流模式:Claude Code 读写剪辑决策,人只看预览页、提修改意见、确认
- 依赖:火山引擎「录音文件识别 2.0」API Key(需注册开通);中文 ASR 效果好,兼做视频字幕来源
- 协议:可商用,内部使用无限制;若将来二次发布派生版本需保留原作者、LICENSE 与 NOTICE.md
- 集成方式:装入 Claude Code 环境即可(与 Remotion skill 并存),`forgecast video` 流程中在渲染前增加可选的 `--cut` 预处理步骤,调用其分镜产物作为 Remotion 模板A 的演示段素材

选配(P2 再评估):inference.sh 的 ai-video-generation skill,接入 Veo/Kling/Seedance 等 40+ 模型,仅用于钩子段 3 秒 B-roll 生成,按调用成本控制用量。明确不引入:HeyGen 数字人、Pexo 全自动成片——与真实感红线冲突(数字人需平台标识且易限流,全自动成片广告感重)。

### 6.5 真实感红线

- 模板A 的演示段**必须用真实录屏**,禁止全片 motion graphics——广告感画面在抖音完播率极差
- 每个项目录屏一次(3-5分钟操作全流程),后续所有视频从这一条素材里切片复用
- BGM 用平台曲库热门音频(发布时在 App 内替换),渲染时只出人声轨

---

## 7. M6 — 素材库与发布辅助

**明确不做:自动发布。** 小红书/抖音对机器发布的风控极严,一封号全盘皆输。本模块只做:

1. `forgecast calendar`:发布日历。规则引擎提示今天该发什么(配比检查 §5.2、单账号每天≤2条、两条同钩子内容间隔≥3天)
2. 素材状态流转:`draft → approved(人工审)→ published(人工发完回填)`
3. 数据回填:`forgecast perf <asset_id> --views=.. --likes=.. --leads=..`,周报统计各钩子类型的询单转化,反哺 M4 模板迭代
4. 询单登记:简单 leads 表(来源素材、微信号、意向、状态),打通"哪条内容带来哪个客户"的归因

---

## 8. M7 — Web 控制台(日常操作主入口)

本地单页应用,`forgecast dev` 一键启动。不做登录、不做权限、不做移动端适配。五个页面:

| 页面 | 功能 | 对应模块 |
|---|---|---|
| **项目看板** | 候选池 Top20 排名表(评分四维雷达)、一键立项/淘汰;立项项目按 stage 泳道展示(analysis→rebranding→producing→publishing→selling)拖拽流转，产物落地（analysis.md/首条素材/发布/询单）自动推进（只前进不回退，手拖仍可任意调）+ 卡片带真实产物计数；立项继承候选期产品说明书/评分明细供未分析时回退展示（2026-08-10） | M1/M2 |
| **项目详情** | 分析/换皮清单双 tab：analysis.md 渲染阅读（未分析时回退展示继承的产品说明书）；换皮清单 tab 一键生成 rebrand-plan.md 并渲染 + 复制全文（喂给 Claude Code 执行改造，2026-08-11）;demo_url、定价、买家画像编辑;raw/ 素材上传(录屏、截图拖拽入库)；「危险操作」删除项目（连带素材/workspace 文件，候选状态重置为可重新立项；素材有关联询单则拒绝，2026-08-11） | M2/M3 |
| **素材工坊** | 核心页面。左侧选项目+钩子类型,点「生成」调 copywriter/studio;右侧素材列表:文案 markdown 预览可**行内编辑**、封面图缩略图、视频内嵌播放;每条素材 draft→approved 一键审核;不满意可「重新生成」并附修改意见(意见拼入提示词重跑)；下方卡点编辑器（原在项目详情页，2026-08-10 移至此处） | M4/M5 |
| **发布日历** | 周视图:按配比规则(§5.2)自动排期建议,已审素材拖到日期格;当天待发清单一键复制文案+下载封面/视频;发完点「已发布」回填平台与链接 | M6 |
| **数据复盘** | 素材表现录入表单(曝光/赞/收藏/询单);钩子类型×转化率周报图表;leads 列表(来源素材归因、跟进状态) | M6 |

实现要点:

- 生成类操作(文案/视频)耗时长,API 用任务队列 + SSE 推进度,前端显示实时日志(视频渲染进度直接透传 Remotion 输出)
- 文件预览:workspace/ 目录由 server 静态托管,视频用原生 `<video>`,markdown 用 react-markdown 渲染
- 所有页面无分页负担——单人数据量,全量拉取 + 前端过滤即可,不过度设计

---

## 9. M3 — 换皮改造流水线(说明)

换皮是 Claude Code 的常规开发工作,不写成代码模块,写成**标准化任务清单生成器**:`forgecast rebrand <slug>` 读取 analysis.md,输出 `rebrand-plan.md`,内容为可直接执行的 checklist:

1. fork 源码 → 全局替换品牌名/Logo/favicon/主题色(给出具体文件路径,由 LLM 分析仓库结构生成)
2. 删除:原项目 GitHub 链接、捐赠链接、英文文档入口、遥测上报
3. i18n:界面全量中文化
4. 新增:analysis.md 建议的 1-2 个本土化功能(微信登录等)
5. 部署:Cloudflare Pages/Workers 或 Docker→轻量服务器,产出 demo_url
6. 录屏:OBS 录制 3-5 分钟全流程操作,存 `raw/`
7. **合规自检:确认无 GPL 传染、无原作者商标残留**

单项目换皮预算:0.5-1 人天(Claude Code 执行)。

---

## 9a. M8 — 定制项目板块(tailor)

> 设计详情见 `docs/superpowers/specs/2026-08-08-tailor-blocks-design.md`(已与用户逐节确认)。本节只记要点。

### 目的

分发营销板块登记的询单(leads)常常带着"能不能定制成 xxx"的意向,此前只能人工承接。M8 打通**询单→接单闭环**:询单一键转为定制需求(`POST /api/leads/:id/to-tailor`,intent 文本带入 raw_need)。同时把接单后"怎么报价、怎么做"固化成流程,核心理念是**避免重复造轮子**:客户需求先拆成能力项,每项到 GitHub 搜可复用开源项目("轮子"),规则打分排出候选,人工逐项决策(选轮子/自研/不做),最后拼出一份**方案书**(选型总表 + 工作量估计 + 风险 + 报价参考)交给人去开发——不自动生成代码。

### 三表结构(packages/core/src/db.ts)

```sql
-- 定制需求
CREATE TABLE tailor_requests (
  id INTEGER PRIMARY KEY,
  title TEXT NOT NULL,
  raw_need TEXT NOT NULL,              -- 客户原始需求(手动粘贴或询单 intent 转入)
  lead_id INTEGER REFERENCES leads(id),-- 询单转入时关联,手动录入为 NULL
  status TEXT DEFAULT 'draft',         -- draft|decomposed|searched|proposed
  proposal_path TEXT,                  -- 方案书文件路径(生成后回填)
  created_at TEXT DEFAULT (datetime('now'))
);

-- 拆出的能力项(可在 Web 上增删改)
CREATE TABLE tailor_capabilities (
  id INTEGER PRIMARY KEY,
  request_id INTEGER REFERENCES tailor_requests(id),
  name TEXT NOT NULL, detail TEXT,
  keywords TEXT,                       -- JSON 数组,GitHub 搜索关键词
  decision TEXT DEFAULT 'pending',     -- pending|wheel|self_build|dropped
  chosen_repo TEXT,                    -- decision=wheel 时的 owner/repo
  sort INTEGER
);

-- 每项能力搜到的候选轮子(协议不合规不删,标 license_ok=0 折叠展示)
CREATE TABLE tailor_wheels (
  id INTEGER PRIMARY KEY,
  capability_id INTEGER REFERENCES tailor_capabilities(id),
  repo TEXT NOT NULL, url TEXT NOT NULL,
  license TEXT, license_ok INTEGER,    -- 复用 scout 的协议白名单判断(isLicenseOk)
  stars INTEGER, last_commit TEXT, description TEXT,
  score REAL, score_detail TEXT        -- 规则评分,JSON 存分项(见下方评分公式)
);
```

产物目录:`workspace/tailor/<id>/proposal.md`,与 `workspace/<slug>/` 每项目一目录的约定平行。

### 状态机

`draft → decomposed → searched → proposed`。动作按状态顺序拦截(未拆解不能搜轮子;能力清单还有 pending 项不能出方案书),路由层(`packages/server/src/app.ts`)与模块内部(`packages/tailor`)双重校验,防止绕过 UI 直调 API 写出半截数据。重新拆解会清掉旧能力项及其轮子(Web 上有确认提示)。

### 两个 LLM capability 及 mock 策略

沿用项目惯例——每个新 LLM capability 自带 mock fixture,不走 `ctx.llm`(它返回的是通用文案 fixture,不是拆解/方案书结果):

- **tailorDecompose**(需求拆解,`packages/tailor/src/decompose.ts`):输入 `raw_need`,输出能力清单(name/detail/keywords)。mock 用 `heuristicDecompose` 按标点切句确定性生成占位能力项;live 走提示词模板 + JSON 解析,解析失败重试一次,再失败报错不写库。
- **tailorProposal**(方案书生成,`packages/tailor/src/proposal.ts`):输入需求 + 已决策能力清单(含选定轮子元数据),输出 `proposal.md`。mock 用 `renderProposalMock` 从已决策数据确定性渲染骨架方案书(选型总表按轮子/自研/不做拼出,协议不合规轮子自动加风险提示段);live 走提示词模板生成完整 markdown,内容过短(<200 字)或调用失败重试一次。

### 轮子评分公式(packages/tailor/src/score.ts `wheelScore`,总分 0-100,纯规则零成本,不烧 LLM——能力数 × 8 轮子量大)

```
score = 活跃 30 + 热度 25 + 协议 15 + 命中 30

活跃(30): last_commit 距今 <90天=30 / <365天=20 / <730天=10 / 否则 0
热度(25): stars ≥1万=25 / ≥1千=20 / ≥100=12 / >0=5 / 否则 0
协议(15): isLicenseOk 白名单=15 / 有协议但非白名单=5 / 无协议=0
命中(30): keywords 命中 repo 名+description 的比例 × 30
```

协议非白名单不打 0——定制场景内部部署(不对外分发)有时可用 GPL 系轮子,人工判断,风险在方案书里点名,不做硬性拦截。

---

## 10. 开发里程碑

### P0 手动验证(第1-2周)⚠️ 先于一切开发

**不写任何抓取代码。** 手动选 2 个项目(建议直接用现成的:全客通/Chatwoot 算一个),手动走完:换皮→录屏→用 Claude 对话生成文案→发布 10-14 条→看询单。
**通过标准:2周内 ≥5 个合格私域咨询。** 不达标先调内容策略,不进 P1。

### P1 流水线 MVP(第3-5周,约7-9个 Claude Code 人天)

| 顺序 | 交付 | 说明 |
|---|---|---|
| 1 | core + SQLite + config + LLM client + server 骨架(Hono)+ Docker Compose(app/renderer 双镜像,§12) | 1.5天,容器化从第一天做,不后补 |
| 2 | M4 copywriter(提示词模板×4钩子 + 封面HTML×3 + dbskill 知识层:sync 命令 + FTS 检索接入提示词) | 2天,**最优先——P0 已验证内容有效,先解决产能** |
| 3 | M7 Web 控制台第一版:素材工坊 + 项目详情两个页面 | 1.5天,先让 M4 的生成/审稿在界面里跑起来 |
| 4 | M5 studio(Remotion 3模板 + TTS)+ 素材工坊接入视频 | 2.5天,模板C→A→B 顺序开发,SSE 渲染进度 |
| 5 | M2 analyst | 0.5天 |
| 6 | M1 scout(抓取+协议过滤+四维评分)+ Web 项目看板页面 | 1.5天,评分权重先按 §3 默认值,成交数据进来后校准 |
| 7 | M6 + Web 发布日历、数据复盘页面 | 1天 |

### P2 自动化补全(第6-7周)

M3 rebrand 清单生成器、周报统计、评分权重按实测数据第一次校准。

### P3 平台化(不排期)

多账号、多人分发、公网部署、登录与权限——**仅在单账号月询单稳定 >30 且交付排队时启动**,届时另立文档。得益于引擎/界面分离架构,平台化只需替换 Web 层与增加多租户,pipeline 引擎不动。

---

## 11. 风险与红线汇总

| 风险 | 对策 | 所在模块 |
|---|---|---|
| GPL/AGPL 传染 | 筛选阶段一票否决;换皮清单含合规自检 | M1/M3 |
| 被扒出"搬运开源" | 换皮最低标准(改名+换UI+demo+功能增减);人设话术定位"基于开源二次开发的产品",被问不否认,主打"我提供部署+定制+售后" | M3/M5.5 |
| AI 内容被平台限流 | 真实录屏为主素材、每天≤2条、人工发布、敏感词本地校验 | M4/M6 |
| 广告法违禁词 | 提示词硬编码规避 + 生成后敏感词表二次校验 | M4 |
| 定制交付压死人 | 优先卖标准化部署(边际成本→0),定制单设起步价过滤低质需求 | 定价策略 |
| 系统建完没询单 | P0 手动验证前置,不达标不开发 | 里程碑 |

---

## 12. 部署与运行(本地 Docker)

整套程序以 Docker Compose 在本机运行,两个服务 + 宿主机挂载:

```yaml
# docker-compose.yml(骨架)
services:
  app:                      # Hono API + Web 静态托管 + CLI 入口
    build: .
    ports: ["127.0.0.1:4321:4321"]   # 只绑 localhost,不暴露局域网
    volumes:
      - ./workspace:/data/workspace   # 项目素材(录屏/文案/封面/成片)
      - ./db:/data/db                 # SQLite 文件
      - ./templates:/data/templates   # 提示词/封面/知识库模板(热更新)
    env_file: .env                    # LLM/GitHub/TTS 等 keys,不进镜像
    depends_on: [renderer]

  renderer:                 # Remotion 渲染 worker
    build: { context: ., dockerfile: Dockerfile.renderer }
    # 镜像内封死 Chromium + ffmpeg + 中文字体(Noto Sans CJK)
    volumes:
      - ./workspace:/data/workspace   # 与 app 共享素材目录
```

要点:

1. **渲染依赖全部封进 renderer 镜像**:Chromium(Remotion 依赖)、ffmpeg、中文字体。这是 Docker 化最大的收益——Remotion 环境在裸机上最容易坏,进了镜像后一次构建处处可跑。渲染任务由 app 经内部队列派发,SSE 进度照常透传到 Web。
2. **数据全部在宿主机 volume**:workspace/(素材)、db/(SQLite)、templates/(模板与 dbskill 知识库)。容器随时可删可重建,资产不丢;备份 = 打包这三个目录。
3. **端口只绑 127.0.0.1**,无认证也不暴露局域网。将来迁到服务器,加一层反代 + Basic Auth 即可,compose 文件不用改。
4. **Claude Code 与 skills 跑在宿主机,不进容器**:换皮开发、videocut 剪辑、dbskill 调用都是宿主机上的 Claude Code 会话,操作对象是挂载的 workspace/ 目录——容器负责"跑系统",Claude Code 负责"干活",两者通过共享目录协作。OBS 录屏也直接存宿主机 workspace/<slug>/raw/。
5. **SQLite 并发注意**:开启 WAL 模式;app 与 renderer 不同时写库(renderer 只读任务、写文件,状态回写走 app 的 API),避免锁冲突。
6. 常用命令:`docker compose up -d` 启动、`docker compose logs -f app` 看日志;`forgecast dev` 在 P1 实现为对 compose 的封装(含首次构建、迁移建表)。

---

## 附录A:forgecast.config.ts 骨架

```typescript
export default {
  llm: {
    baseURL: "https://aitoken.homes/v1",
    apiKey: process.env.FORGECAST_LLM_KEY,
    models: { analysis: "<长上下文模型>", copy: "<写作强的模型>", scoring: "<便宜的模型>" }
  },
  tts: { provider: "minimax", voiceId: "", fallback: "edge-tts" },
  github: { token: process.env.GH_PAT, topics: ["crm","e-commerce","live-chat", /*...*/] },
  scoring: { weights: { rebrandCost: 30, buyerClarity: 40, visual: 30 } },
  publish: { maxPerDay: 2, mix: { demo: 0.6, story: 0.2, dev: 0.2 } }
}
```

## 附录B:给 Claude Code 的启动指令(P1 开始时使用)

> 按《开源变现内容工厂-开发文档》搭建 forgecast monorepo。先完成里程碑 P1 的第 1-3 项(core+server 骨架、copywriter、Web 控制台的素材工坊与项目详情页)。提示词模板先出骨架,四个钩子模板参照 §5.1 公式各写一版,我会基于 P0 实测数据迭代。完成后 `forgecast dev` 启动,用 workspace/demo-project 在素材工坊页面跑通「生成文案→行内编辑→审核通过」全流程给我看。
